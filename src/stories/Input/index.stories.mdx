import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import "assets/css/index.css";
import Input from "./example/Sample";

<Meta title="Components/Input" />

# Input

A most import components that simply let user interact with the app,[[Default Example](/docs/components-input-example--default)] if you want to see in action

export const Template = (args) => <Input {...args} />;

<Story name="Introduction" args={{}}>
  {Template.bind({})}
</Story>
